import { Box, Button, Card, CardActions, Typography } from "@mui/material";
import React from "react";

const Footer = () => {
  return;
  <Card sx={{ maxWidth: 345 }}>
    <CardActions>
      <p>¿No querés ver esto?</p>;<Button size="small">Saltar</Button>
    </CardActions>
  </Card>;
};

export default Footer;
